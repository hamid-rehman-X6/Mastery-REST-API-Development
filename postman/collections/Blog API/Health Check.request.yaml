$kind: http-request
name: Health Check
description: |-
  Returns the API health status including version and timestamp.
  
  **Access:** Public (no authentication required)
method: GET
url: '{{baseUrl}}/api/v1'
order: 1000
